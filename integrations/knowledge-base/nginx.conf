server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # The App
    location / {
        try_files $uri $uri/ /index.html;
    }

    # The Data (Autolisting as JSON)
    location /files/ {
        autoindex on;
        autoindex_format json;
    }
}
